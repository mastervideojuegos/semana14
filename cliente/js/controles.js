function controles(){
    /*// Gestión de las teclas
    $(document).keypress(function(event){
        // Esto es lo que pasa cuando pulso la W
        if ( event.which == 119 ) {player[0].avance = 1;player[0].aceleracion=1;}
        // Esto es lo que pasa cuando pulso la S
        if ( event.which == 115 ) {player[0].avance = -1;player[0].aceleracion=0.75;}
        // Esto es lo que pasa cuando pulso la A
        if ( event.which == 97 ) {player[0].girando = 1;}
        // Esto es lo que pasa cuando pulso la D
        if ( event.which == 100 ) {player[0].girando = -1;}
    });
    //Si Pulsas Espacio dispara
    $(document).keydown(function(event) {
        if (event.which == 32) {
            //console.log("Pulsado")
            player[0].sprint=1;
            //sonido.playLaser();
          
        }
    });
    
     $(document).keyup(function(event){
         // Esto es lo que pasa cuando pulso la W
        if ( event.which == 87 ) {player[0].avance = 0;}
        // Esto es lo que pasa cuando pulso la S
        if ( event.which == 83 ) {player[0].avance = 0;}
        // Esto es lo que pasa cuando pulso la A
        if ( event.which == 65 ) {player[0].girando = 0;}
        // Esto es lo que pasa cuando pulso la D
        if ( event.which == 68 ) {player[0].girando = 0;}
        // Esto es lo que pasa cuando levanto el espacio
        if ( event.which == 32 ) {player[0].sprint = 0;}
    });*/
}